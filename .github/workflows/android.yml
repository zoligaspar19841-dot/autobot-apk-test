name: Build APK

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      ANDROIDSDK: /home/runner/.buildozer/android/platform/android-sdk
      ANDROIDAPI: 31
      ANDROIDMINAPI: 21

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install deps
        run: |
          sudo apt update
          sudo apt install -y zip unzip openjdk-17-jdk python3-pip wget

      - name: Prepare Android SDK exactly for Buildozer
        run: |
          mkdir -p "$ANDROIDSDK/tools"
          cd "$ANDROIDSDK"
          wget -q https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -O cmdtools.zip
          unzip -q cmdtools.zip -d cmdtmp
          cp -r cmdtmp/cmdline-tools/* "$ANDROIDSDK/tools/"
          test -f "$ANDROIDSDK/tools/bin/sdkmanager"
          mkdir -p "$ANDROIDSDK/licenses"
          printf "%s\n" \
            "8933bad161af4178b1185d1a37fbf41ea5269c55" \
            "d56f5187479451eabf01fb78af6dfcb131a6481e" \
            "24333f8a63b6825ea9c5514f83c2829b004d1fee" \
            > "$ANDROIDSDK/licenses/android-sdk-license"
          printf "%s\n" \
            "84831b9409646a918e30573bab4c9c91346d8abd" \
            > "$ANDROIDSDK/licenses/android-sdk-preview-license"
          yes | "$ANDROIDSDK/tools/bin/sdkmanager" --sdk_root="$ANDROIDSDK" --licenses || true
          "$ANDROIDSDK/tools/bin/sdkmanager" --sdk_root="$ANDROIDSDK" \
            "platform-tools" \
            "platforms;android-31" \
            "build-tools;31.0.0"

      - name: Install buildozer
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install buildozer cython

      - name: Build APK
        run: |
          buildozer -v android debug

      - name: Find APK
        run: |
          find . -name "*.apk" -type f
          test -n "$(find . -name '*.apk' -type f | head -n 1)"

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: apk
          path: |
            bin/*.apk
            **/*.apk
          if-no-files-found: error

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs
          path: |
            .buildozer/**/*.log
            *.log
          if-no-files-found: ignore
